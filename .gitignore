# Dependencies (when a build step is added later)
node_modules/

# Build output
dist/
build/

# Environment / secrets
.env
.env.*
!.env.example

# Python (used by the ui-ux-pro-max skill scripts)
__pycache__/
*.py[cod]

# OS / editor cruft
Thumbs.db
.DS_Store
*.log

# Editor settings: keep local, but share settings.json (prompt-file discovery)
.vscode/*
!.vscode/settings.json

# Local corporate CA bundle for Node TLS (machine-specific)
apps/api/.local-ca.pem
